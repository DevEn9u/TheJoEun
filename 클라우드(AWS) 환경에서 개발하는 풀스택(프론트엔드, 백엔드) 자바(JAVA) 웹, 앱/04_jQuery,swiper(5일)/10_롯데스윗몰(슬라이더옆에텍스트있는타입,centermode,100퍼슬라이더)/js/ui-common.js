$(function() {
  let mainNew = new Swiper('.main_new .swiper', {
    // css로 크키 지정시 auto
    slidesPerView: 'auto',
    spaceBetween: 30,

    scrollbar: {
      el: '.swiper-scrollbar',
    },
  });
});